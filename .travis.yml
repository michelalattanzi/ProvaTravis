language: java
jdk:
   -openjdk8
addons:
  apt:
    packages:
      - ant
script: ant build

addons:
  sonarcloud:
    organization: "michelalattanzi"
    token:
      secure: "19d780e3e3818c83d2ea3102f3422b7082d76e0d" # encrypted value of your token

script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar -Dsonar.projectKey=michelalattanzi_ProvaTravis